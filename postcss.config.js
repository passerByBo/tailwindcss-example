export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
    // tailwindcss: { config: './tailwindcss-config.js' },
    // ...(process.env.NODE_ENV === "production" ? { cssnano: {} } : {}),
  },
};
